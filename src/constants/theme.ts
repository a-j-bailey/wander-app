import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    small: {
        fontSize: 8,
        color: 'grey'
    },
    container: {
        flex: 1,
    },
    contentContainer: {
        flex: 1,
        alignItems: 'center',
    },
});

export default styles;